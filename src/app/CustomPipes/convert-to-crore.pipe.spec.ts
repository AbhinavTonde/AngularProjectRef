import { ConvertToCrorePipe } from './convert-to-crore.pipe';

describe('ConvertToCrorePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToCrorePipe();
    expect(pipe).toBeTruthy();
  });
});
